import {readData, writeData, writeComponent, readComponent } from "./localStorage";

module.exports = {
    readData: readData,
    writeData: writeData,
    readComponent: readComponent,
    writeComponent: writeComponent
}