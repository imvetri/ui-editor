git status
npm run build
git status
git add .
git commit -m "editable label for new custom nodes works smooth"
git branch
git push
cd offgrid
ls
cd ui-editor
nvm
export NVM_DIR="$([ -z "${XDG_CONFIG_HOME-}" ] && printf %s "${HOME}/.nvm" || printf %s "${XDG_CONFIG_HOME}/nvm")"\n[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" # This loads nvm
nvm
nvm use 14
npm start